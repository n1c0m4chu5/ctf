### Forensics:
# Disk Forensics:

- debugfs
- foremost
- photorec
- testdisk
- Fix Magic Number ('signature'): https://wiki.osdev.org/Ext2#Superblock

### Binary Exploitation:
# Unsafe C-functions and their replacements:

    strcpy -> strncpy -> strlcpy/strcpy_s
    strcat -> strncat -> strlcat/strcat_s -strtok
    strcmp -> strncmp
    sprintf -> snprintf
    vsprintf -> vsnprintf
    gets -> fgets/gets_s
    makepath -> _makepath_s (MSDN)
    _splitpath -> _splitpath_s (MSDN)
    scanf/sscanf -> sscanf_s (MSDN)
    snscanf -> _snscanf_s (MSDN)
    strlen -> strnlen_s (MSDN)

### Pwn
# Linux Enumeration: https://github.com/rebootuser/LinEnum
## See what you can run as root
# Linux vulnerable binaries and commands: GTFObins https://gtfobins.github.io/
# Basic Privilege Escalation https://blog.g0tmi1k.com/2011/08/basic-linux-privilege-escalation/

# Windows vulnerable binaries and commands: LOLBAS https://github.com/LOLBAS-Project/LOLBAS

